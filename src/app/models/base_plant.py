# 

"""Base plant model representing database table. Example:

from sqlalchemy import Integer, Column, String

class: BasePlant:
    id  = Column(Integer, primary_key=True)
    name = Column(String, primary_key=True, nullable=False)
"""